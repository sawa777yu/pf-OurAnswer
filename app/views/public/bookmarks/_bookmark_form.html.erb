<% if user_signed_in? %>

  <% if post.bookmarked_by?(current_user) %>
    <p>
      <%= link_to "unbookmark",post_bookmarks_path(post), method: :delete, class: 'btn btn-danger btn-sm', remote: true %>
      ブックマーク数：<%= post.bookmarks.count %>
    </p>
    <% else %>
    <p>
      <%= link_to "bookmark",post_bookmarks_path(post), method: :post, class: 'btn btn-primary btn-sm', remote: true %>
      ブックマーク数：<%= post.bookmarks.count %>
    </p>
  <% end %>

<% end %>